services:
  fintrack:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fintrack
    restart: unless-stopped
    ports:
      - "3400:3000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
